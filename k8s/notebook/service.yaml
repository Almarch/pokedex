apiVersion: v1
kind: Service
metadata:
  name: notebook
  labels:
    app: notebook
spec:
  type: ClusterIP
  selector:
    app: ollama
  ports:
    - port: 8888
      targetPort: 8888
      protocol: TCP